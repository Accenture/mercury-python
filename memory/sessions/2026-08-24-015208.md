# Session (2026-08-24T01:52:08.000Z)

**Agent:** Claude Code

**Actuator polish — Eric's three loose ends, engine signatures extracted from the Java
repo first.** Branch `feature/actuator-polish`, commit `da60593`; node twin `956c706`
(rides its chore/quality-polish branch). Gates 57/57 + ruff + basedpyright 0; live drive
verified all three shapes. Pending Eric's PR gate.

## The three items (engine facts pinned)

1. **`GET /` index page** — the engines' minimal Welcome page
   (platform-core `public/index.html`: h2 + endpoint links) embedded as a constant —
   deliberately NO static file service. Links: /info, /info/routes, /env, /health,
   /livenessprobe.
2. **Pretty-printed JSON** — the Java default serializer is pretty
   (`SimpleMapper`: Gson `setPrettyPrinting` + disableHtmlEscaping, 2-space); Rust was
   updated to match; wrappers now render actuator JSON with indent 2
   (`json.dumps(indent=2, ensure_ascii=False)` / `JSON.stringify(body, null, 2)`).
3. **Host error shape** — the engines' `SimpleHttpUtility.sendResponse` puts
   `status`, `message`, `type: "error"` (that insertion order); unknown-path message is
   `Resource not found` (HttpRequestHandler + ActuatorServices). Wrappers now answer
   that shape for unknown paths AND non-GET methods on known paths (python: catch-all
   route + method gate in the dispatcher — aiohttp's default text page never renders;
   node: server's not-found/error branches use the shared `sendError`).

Envelope wire transport errors are untouched (they ride envelope bytes, engine
semantics). Liveness stays text/plain (engine parity). Pins: index page links, 404
shape (GET unknown + POST /info), pretty rendering (`{\n  "app"`). SIM117 lesson:
combined async-with can reference earlier as-names.

## Memory References

- created: thread-actuator-polish
- referenced: thread-actuator-endpoints, bp-publish-interop-gate
