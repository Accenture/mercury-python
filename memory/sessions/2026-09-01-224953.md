# Session (2026-09-01T23:45:00.000Z)

**Agent:** Claude Code
**Round:** Release v4.12.1 + the FIRST PyPI publication — **COMPLETE and verified
2026-09-01.** PR #26 merge `af86a0ba` carrying gated `edf3463` (tree verified); tag
v4.12.1; `pip install mercury-composable` is live.

## What happened

- **Publication metadata:** SPDX license expression + `license-files` (PEP 639, replacing
  the file-table form); trove classifiers (Beta, Python 3.10–3.14, AsyncIO); keywords;
  project URLs corrected (Documentation now points at THIS repo's site, not the
  engine's) and extended (Repository, Changelog). Version 4.12.1 in pyproject +
  `version.py`; CHANGELOG flipped (the 4.12.1 section carries llm.chat/llm.stream).
- **Constrained sdist (the important fix):** hatchling's default sdist ships the whole
  repo — including `memory/`, `agent-skills/` and CI plumbing. `only-include` now pins
  it to package sources, tests, examples and top-level docs.
- **Verification:** 97 tests + ruff green; build + `twine check` PASSED; sdist contents
  inspected; wheel smoke test in a fresh venv (python 3.14) — and the 3.9 system python
  was correctly REJECTED by `requires-python`, proving the floor. Post-publish: live
  `pip install mercury-composable==4.12.1` from PyPI → import at 4.12.1,
  `mercury-serve --help` works; index metadata verified (license expression, URLs).

## Durable lesson

**An agent-memory-enabled repo publishing to PyPI MUST constrain its sdist** — the
default ships the shared memory layer and skills to the registry. `only-include` (or
equivalent) is part of publication readiness, same class as the node repo's `files`
allowlist and the Rust crates' `publish = false` fences.

## Memory References

- Referenced: ot-llm-ai-nodes (ships in this first public package),
  stack-python-hatchling
- Created: (none)
- Reactivated: (none)
- Superseded: (none)
