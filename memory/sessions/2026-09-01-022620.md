# Session (2026-09-01T02:41:40.000Z)

**Agent:** Claude Code
**Round:** AI nodes `llm.chat` + `llm.stream` (agent-orchestration experiment E0, with the
engine repo's same-day round) — **MERGED 2026-09-01 as
[PR #22](https://github.com/Accenture/mercury-python/pull/22) true merge `44caf9a6`
carrying gated `1fa5f70`; tree verified; branches deleted both ends.**

## What happened

- **`llm.chat`** (examples/demo_app.py, route `llm.chat`, instances 50): provider-neutral
  LLM adapter — prompt|messages + optional system + optional JSON schema → structured
  output (`additionalProperties` defaults false; the parseable verdict shape a graph's
  decision routing needs); returns text|data, model, stop_reason, usage. Provider errors
  ride the envelope status (429/status/503 — portable to a graph's error context);
  `params.timeout_ms` → SDK timeout (the x-ttl pattern). Provider selection:
  params.provider → `llm.provider` config key → anthropic; models default per provider
  (`claude-opus-5` / `gemini-3.6-flash`).
- **`llm.stream`** (interceptor + EventStreamWriter): pulls the provider's REAL token
  stream and relays each batch over the multi-shot reply contract; terminal metadata
  carries model/stop_reason/usage + trace and business correlation ids. Schema is
  deliberately NOT part of the streaming contract (a verdict is single-shot — use
  llm.chat).
- **Two provider editions, one contract:** Anthropic (`messages.create` /
  `messages.stream`) and Gemini (`generate_content` / `generate_content_stream`), SDKs as
  lazy optional extras — `pip install 'mercury-composable[llm]'` (`anthropic>=1,<2`,
  `google-genai>=2,<3`).
- **Gemini AFC opt-out:** google-genai enters its automatic-function-calling loop by
  default even with zero tools (advisory warning on every direct call). Both editions set
  `AutomaticFunctionCallingConfig(disable=True)` — truthful (the AI nodes expose no tool
  surface; the graph decides, the model advises). `AsyncChat.send_message` considered and
  REJECTED: SDK-side hidden agency + a stateful per-process conversation object conflict
  with bounded agency, 50 stateless instances, and graph suspend/resume.
- **Sonar/IDE round (Eric's scan → zero):** `TEXT_EVENT_STREAM` constant (S1192);
  `_gemini_request`/`_gemini_finish`/`_gemini_usage` extraction killed two S3776 findings
  AND the chat/stream duplication; 9 composite assertions split;
  `isinstance(out.error, AppException)` replaced `# type: ignore` comments.
- **Durable lesson (PyCharm + monkeypatch):** PyCharm validates the literal attribute-name
  string in `monkeypatch.setattr(obj, "name", v)` regardless of the target's typing (an
  `Any` annotation does NOT silence it), and it statically resolves dotted-string targets
  (a sys.modules-registered synthetic name is "unresolved"). The fix that works: pass the
  name through a helper parameter — `_patch_demo(monkeypatch, attr, value)` — a variable
  is statically opaque; monkeypatch still validates at run time and restores on teardown.
- **Tests:** 14 new token-free tests (fake provider clients + recording writer) — suite
  97 passed; ruff + basedpyright clean. Live evidence with the engine (real Gemini
  verdicts through the support-triage graph; 25+ token batches out the engine's SSE edge;
  one distributed trace across processes) is recorded in the engine repo
  (mercury-composable log 2026-09-01-022524; its PR #304).

## Memory References

- Referenced: stack-python-hatchling, stack-deps-aiohttp-msgpack, stack-mercury-serve-cli,
  ot-streaming-telemetry-round-20260830
- Created: ot-llm-ai-nodes (tier: working)
- Reactivated: (none)
- Superseded: (none)
